# Scope pytest to this package: run `pytest` from this directory.
# Without this, pytest anchors at the repo's pyproject.toml and resolves the
# tests through the top-level foggie package, whose __init__ imports the
# full analysis stack (matplotlib, yt, ...) that the IC tools don't need.
[pytest]
consider_namespace_packages = false
testpaths = tests
